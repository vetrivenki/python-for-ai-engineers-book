[pytest]
# Chapter files are numbered (ch2_10_test_prompt_lab.py), which does not match
# pytest's default test_*.py discovery pattern. Widen it so `pytest` just works.
python_files = ch2_*test*.py test_*.py
