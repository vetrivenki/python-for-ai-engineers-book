[pytest]
# The chapter files are numbered (ch1_10_test_client.py), which does not match
# pytest's default test_*.py discovery pattern. Widen it so `pytest` just works.
python_files = ch1_*test*.py test_*.py
